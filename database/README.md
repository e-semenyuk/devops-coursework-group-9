# Database import

Place the coursework-provided MySQL `world` database import file in this directory, using a `.sql` extension. Docker Compose automatically imports SQL files from this directory when the database volume is created for the first time.

Do not commit credentials, private exports, or an unverified database copy. The team should first confirm which source/version was provided by the module.
